# Autotools-HelloWorldLib
Autotools example project.

# How to use

## Bootstrap the toolchain
./scripts/setup.sh

## Build the example
./scripts/build.sh

Configured to generate a static library in the lib folder.
